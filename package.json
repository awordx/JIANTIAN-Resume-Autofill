{
  "name": "resume-pro-plugin",
  "private": true,
  "version": "0.3.0",
  "description": "Dev tooling for the Resume Pro browser extension. The extension itself ships no npm dependencies and has no build step; this package exists only for type checking and protocol type generation.",
  "scripts": {
    "gen:types": "node desktop/crates/protocol/js/gen-types.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.278",
    "typescript": "^5.7.2"
  }
}
